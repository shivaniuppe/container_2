version: latest
services:
  container_2:
    platform: linux/amd64
    image: gcr.io/cloud-429207/container_2:latest
    container_name: container_2
    ports:
      - "6001:6001"
    networks:
      - app-network
    volumes:
      - .:/data

networks:
  app-network:
    external: true
